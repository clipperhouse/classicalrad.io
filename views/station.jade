extends layout

block content
	.station
		h1 #{station.name} 
			span #{station.location}
		audio(src='#{station.stream_url}', type='audio/mp3', controls='controls')
	p
		a(href='/') home
	script
		$('audio,video').mediaelementplayer();